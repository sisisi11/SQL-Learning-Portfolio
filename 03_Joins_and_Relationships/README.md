# Week 3 — JOINs & Relationships

At this point the database is expanded with two simple tables:

- Payments
- Returns

Run `00_Add_Payments_and_Returns.sql` once before the Week 3 exercises.

This is where relational querying becomes useful rather than theoretical.
